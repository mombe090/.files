# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE FOR POWERSHELL
# =============================================================================
# Copy this file to $HOME/.config/powershell/env.local.ps1 and fill in your actual values
# This file should be loaded in your Microsoft.PowerShell_profile.ps1 with: . $PSScriptRoot/env.local.ps1
#
# IMPORTANT: Never commit the actual env.local.ps1 file with real credentials!
# =============================================================================

# ===== USER CONFIGURATION =====
# Used for git config generation and personal setup
$env:USER_FULLNAME = "Your Full Name"
$env:USER_EMAIL = "your.email@example.com"
$env:PERSONAL_USER = "your-username"

# ===== MACHINE TYPE =====
# Set to "pro" for work machines, "perso" for personal machines
$env:PC_TYPE = "perso"  # Options: pro | perso

# ===== OPTIONAL: AWS REGION =====
# Uncomment if you use AWS CLI
# $env:AWS_DEFAULT_REGION = "us-east-1"

# ===== OPTIONAL: KUBERNETES =====
# Uncomment if you use kubectl with custom config
# $env:KUBECONFIG = "$HOME\.kube\config"

# ===== OPTIONAL: OPENAI / AI SERVICES =====
# Uncomment if you use AI APIs
# $env:OPENAI_API_KEY = "YOUR_OPENAI_API_KEY"
# $env:ANTHROPIC_API_KEY = "YOUR_ANTHROPIC_API_KEY"

# ===== CUSTOM VARIABLES =====
# Add your own environment variables below

# ===== LOAD PRIVATE VARIABLES =====
# Source private environment variables from env.private.ps1 if it exists
# This file should contain your sensitive credentials (API keys, tokens, passwords)
$envPrivate = Join-Path $PSScriptRoot "env.private.ps1"
if (Test-Path $envPrivate) {
    . $envPrivate
}

# ===== NOTES =====
# 1. Copy this file:
#    Copy-Item env.ps1.sample $HOME/.config/powershell/env.local.ps1
#
# 2. Edit with your values:
#    code $HOME/.config/powershell/env.local.ps1
#
# 3. Add to your PowerShell profile (if not already present):
#    $envLocal = Join-Path $PSScriptRoot "env.local.ps1"
#    if (Test-Path $envLocal) {
#        . $envLocal
#    }
#
# 4. Reload profile:
#    . $PROFILE
#
# ⚠️  SECURITY WARNING: Never commit env.local.ps1 with real credentials!
#     Add to .gitignore: "env.local.ps1" | Out-File .gitignore -Append
