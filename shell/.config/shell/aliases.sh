# =============================================================================
# Shared Shell Aliases
# =============================================================================
# Common aliases shared between bash and zsh
#
# Usage:
#   Source from bash: [[ -f ~/.config/shell/aliases.sh ]] && source ~/.config/shell/aliases.sh
#   Source from zsh: [[ -f ~/.config/shell/aliases.sh ]] && source ~/.config/shell/aliases.sh
# =============================================================================

# ===== GIT FORGE CLI TOOLS =====

# GitHub CLI
alias ghpr='gh pr'
alias ghissue='gh issue'
alias ghrepo='gh repo'

# Gitea CLI (tea)
alias t='tea'
alias tpr='tea pr'
alias tissue='tea issues'
alias trepo='tea repos'
