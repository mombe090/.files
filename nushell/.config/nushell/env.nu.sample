# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE FOR NUSHELL
# =============================================================================
# Copy this file to ~/.config/nushell/env.local.nu and fill in your actual values
# This file should be loaded in your env.nu with: source env.local.nu
#
# IMPORTANT: Never commit the actual env.local.nu file with real credentials!
# =============================================================================

# ===== USER CONFIGURATION =====
# Used for git config generation and personal setup
$env.USER_FULLNAME = "Your Full Name"
$env.USER_EMAIL = "your.email@example.com"
$env.PERSONAL_USER = "your-username"

# ===== MACHINE TYPE =====
# Set to "pro" for work machines, "perso" for personal machines
$env.PC_TYPE = "perso"  # Options: pro | perso

# ===== OPTIONAL: AWS REGION =====
# Uncomment if you use AWS CLI
# $env.AWS_DEFAULT_REGION = "us-east-1"

# ===== OPTIONAL: KUBERNETES =====
# Uncomment if you use kubectl with custom config
# $env.KUBECONFIG = $"($env.HOME)/.kube/config"

# ===== OPTIONAL: OPENAI / AI SERVICES =====
# Uncomment if you use AI APIs
# $env.OPENAI_API_KEY = "YOUR_OPENAI_API_KEY"
# $env.ANTHROPIC_API_KEY = "YOUR_ANTHROPIC_API_KEY"

# ===== CUSTOM VARIABLES =====
# Add your own environment variables below

# ===== LOAD PRIVATE VARIABLES =====
# Source private environment variables from env.private.nu if it exists
# This file should contain your sensitive credentials (API keys, tokens, passwords)
let env_private = ($nu.default-config-dir | path join "env.private.nu")
if ($env_private | path exists) {
    source $env_private
}

# ===== NOTES =====
# 1. Copy this file: cp env.nu.sample ~/.config/nushell/env.local.nu
# 2. Edit with your values: nvim ~/.config/nushell/env.local.nu
# 3. Add to your env.nu: source env.local.nu
# 4. Restart Nushell
#
# ⚠️  SECURITY WARNING: Never commit env.local.nu with real credentials!
#     Add it to .gitignore: echo "env.local.nu" >> ~/.config/nushell/.gitignore
